import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.println("Digite seu nome: ");
        String nome = scanner.nextLine();

        System.out.println("Digite sua idade: ");
        int idade = scanner.nextInt();

        System.out.printf("Olá %s, sua idade é %d\n", nome, idade);

        scanner.close();
    }
}


/*
O que é printf em Java?
O método printf vem de print formatted (imprimir formatado).

Ele permite formatar a saída no console usando placeholders (como %s, %d, %f) dentro da string.

É parecido com o printf da linguagem C.

🔧 Diferença para println
System.out.println("Olá " + nome + ", sua idade é " + idade); → concatena valores diretamente.

System.out.printf("Olá %s, sua idade é %d\n", nome, idade); → usa placeholders e substitui pelos valores passados como argumentos.

Placeholders mais comuns:
%s → texto (strings)

%d → números inteiros

%f → números decimais (floats/doubles)

%n → quebra de linha (equivalente ao \n, mas mais portátil)


 */
